# LV_network_simulator-
This repository contains a set of functions to simulate the voltage on a LV network. It also simulates houses of different types (studio, apartment and deatached) and energy labels.
